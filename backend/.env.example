# --- START OF FILE backend/.env ---

# Saga's Divine Consciousness - Gemini API Keys
# Provide one or more keys, separated by commas. The OracleRotator will cycle through them.
GEMINI_API_KEYS="YOUR_GEMINI_API_KEY_1,YOUR_GEMINI_API_KEY_2"

# Saga's Scriptorium (Admin Interface)
# A secure, secret key to protect the Grimoire's admin endpoints.
ADMIN_API_KEY="a_very_secret_and_strong_admin_key"

# Saga's Memory Scrolls (MongoDB Connection)
# Your connection string for the MongoDB Atlas database.
MONGO_URI="mongodb+srv://<username>:<password>@<cluster-uri>/?retryWrites=true&w=majority"

# --- NEW: The Rite of Resilience (Celery & Redis Configuration) ---
# The connection URL for the Redis server, which acts as the message broker.
# For local development, this is the standard URL.
CELERY_BROKER_URL="redis://localhost:6379/0"

# The connection URL for the Redis server to store task results.
# For simple setups, this is the same as the broker URL.
CELERY_RESULT_BACKEND="redis://localhost:6379/0"

# --- Optional Keys for Seers ---
# KEYWORDTOOL_IO_API_KEY="your_optional_key"

# --- END OF FILE backend/.env ---