# .env - Environment variables for the NicheStack AI Backend

# --- REQUIRED ---

# Your Google Gemini API Key for AI-driven analysis.
# Get this from Google AI Studio (https://aistudio.google.com/app/apikey).
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"

# Your MongoDB Connection String.
# Used to store request and response data for analytics and history.
# For local development: MONGO_URI="mongodb://localhost:27017"
# For production (e.g., MongoDB Atlas): MONGO_URI="mongodb+srv://<username>:<password>@<cluster-url>/nichestack_db?retryWrites=true&w=majority"
MONGO_URI="YOUR_MONGODB_CONNECTION_STRING_HERE"


# --- OPTIONAL ---

# Your API Key for KeywordTool.io (https://keywordtool.io/api).
# If you do not have a subscription, leave this blank.
# The system will gracefully skip keyword suggestions from this source if the key is missing.
KEYWORDTOOL_IO_API_KEY=""

# Your API Key for an IP Geolocation service.
# This is used for automatic country detection based on the user's IP.
# Example service: ipinfo.io.
# If left blank, country detection will only work if the user manually selects a country from the dropdown.
IP_GEOLOCATION_API_KEY=""